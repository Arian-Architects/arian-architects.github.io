---
const { name, twitter, image } = Astro.props
---

<div class="flex flex-row items-center space-x-3">
  <img class="rounded-full" width={30} height={30} alt={name} title={name} src={image} />
  <div class="flex flex-col">
    <span class="text-sm">{name}</span>
    <a class="text-xs text-blue-500" href={'https://twitter.com/' + twitter} target="_blank">@{twitter}</a>
  </div>
</div>
